# متجر ANJUM — الباك اند

هاد مشروع سيرفر حقيقي (Node.js) لمتجر ANJUM. فيه:
- صفحة المتجر للزباين (`/`)
- لوحة تحكم منفصلة تماماً (`/admin`) — مش مرتبطة أو ظاهرة بموقع الزباين
- تخزين المنتجات بملف بسيط (`data/products.json`) — بدون قاعدة بيانات معقدة
- رفع صور المنتجات وحفظها كملفات حقيقية
- **سلة مشتريات + إتمام طلب**: الدفع عند الاستلام أو تحويل كليك (مع رفع صورة إشعار التحويل)
- **تتبع الطلب**: الزبون يقدر يشوف حالة طلبه برقم هاتفه، بدون تسجيل دخول
- **إدارة الطلبات بلوحة التحكم**: تشوف كل الطلبات وتغير حالتها (قيد المعالجة / تم الشحن / تم التسليم...) وتشوف إشعار تحويل الكليق لو موجود

### رقم الكليق
الرقم الافتراضي المضبوط حالياً هو **٠٧٧٦٤١٠٠٥٩**. تقدر تغيره من ملف `.env` (متغير `CLIQ_ALIAS`) بدون لمس الكود.

---

## ١) تجربته على جهازك أول (اختياري بس منصوح فيه)

لازم يكون عندك Node.js مثبت (نزّله من nodejs.org إذا مش موجود).

افتح Terminal / Command Prompt جوا مجلد المشروع واكتب:

```
npm install
cp .env.example .env
```

افتح ملف `.env` يلي انطبع، وغيّر:
- `ADMIN_PASSWORD` — حط الباسورد يلي بدك تدخل فيه عاللوحة
- `SESSION_SECRET` — حط أي جملة عشوائية طويلة

بعدين شغّل السيرفر:

```
npm start
```

افتح المتصفح على:
- `http://localhost:3000` — صفحة المتجر
- `http://localhost:3000/admin` — لوحة التحكم

---

## ٢) رفعه لاستضافة حقيقية (Deploy)

بما إنك رح تشتري الدومين لحالك، أسهل طريقة تنزل هاد المشروع "لايف" هي منصة **Render.com** (في خطة مجانية، وسهلة كثير):

### الخطوات:

1. اعمل حساب مجاني على **render.com**
2. ارفع مجلد المشروع هاد كـ **repository** على GitHub (اعمل حساب GitHub إذا ما عندك، وارفع الملفات — فيه فيديوهات كثير عاليوتيوب تشرح "how to upload a folder to GitHub" بالعربي والإنجليزي)
3. من لوحة Render اضغط **New > Web Service**، واختار الـ repository يلي رفعته
4. بالإعدادات:
   - **Build Command:** `npm install`
   - **Start Command:** `npm start`
5. تحت **Environment Variables** ضيف:
   - `ADMIN_PASSWORD` = الباسورد يلي بدك ياه
   - `SESSION_SECRET` = جملة عشوائية طويلة
6. اضغط **Create Web Service** وانتظر شوي لحد ما يخلص الرفع
7. رح ياخذك رابط شبيه بـ `https://anjum-store.onrender.com` — جرب افتحه

### ⚠️ ملاحظة مهمة جداً عن حفظ البيانات

الخطة **المجانية** من Render (وأغلب الاستضافات المجانية) بتمسح الملفات يلي اتغيرت (زي المنتجات الجديدة والصور) كل ما يعيد تشغيل السيرفر تلقائياً. يعني منيح للتجربة، بس مش مضمون للاستخدام الحقيقي طويل المدى.

**للاستخدام الحقيقي والدائم** لازم توصل خدمة اسمها **Persistent Disk** (قرص دائم) وهاي غالباً بكلفة بسيطة شهرياً (حوالي ١-٧ دولار). بالإعدادات بتضيف Disk وتربطه بمسار `data` و `public/uploads` عشان البيانات ما تنمسح.

بدائل ثانية غير Render: **Railway.app** أو **Fly.io** — نفس الفكرة تقريباً.

---

## ٣) ربط الدومين يلي بتشتريه

بعد ما يصير عندك رابط من Render (أو أي استضافة تانية):
1. من لوحة تحكم الدومين (مثلاً من الشركة يلي اشتريته منها زي GoDaddy أو Namecheap)
2. روح على إعدادات **DNS**
3. ضيف سجل من نوع **CNAME** يوجه الدومين تبعك لرابط Render
4. من لوحة Render، تحت **Settings > Custom Domain**، ضيف نفس الدومين

كل استضافة بتشرح هاي الخطوة بالتفصيل بصفحة "Custom Domain" تبعها.

---

## ٤) شو رابط لوحة التحكم؟

بعد ما يصير الموقع لايف، رابط لوحة التحكم رح يكون:

```
https://دومينك.com/admin
```

هاد الرابط مش مكتوب أو ظاهر بأي مكان بموقع الزباين — بس أنت يلي بتعرفه.

---

## أسئلة متوقعة

**ليش ما في قاعدة بيانات حقيقية؟**
لأنك طلبت أبسط حل ممكن. البيانات محفوظة بملف JSON بسيط عالسيرفر، وهاد كافي لمتجر بحجم متوسط. إذا المتجر كبر كثير بالمستقبل (آلاف المنتجات ومبيعات يومية)، ساعتها بينصح تنتقل لقاعدة بيانات حقيقية.

**هل لازم أعرف برمجة عشان أشغله؟**
لأ، بس بتحتاج تتبع الخطوات فوق بالترتيب. إذا تعقدت بأي خطوة، اسألني بالتفصيل وبساعدك.
